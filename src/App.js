import React from 'react';

import RecipeBox from './Components/RecipeBox';
import './App.sass';

const App = (props) => {
  return (
      <div>
        <RecipeBox />
      </div>
    );
  }

export default App;
